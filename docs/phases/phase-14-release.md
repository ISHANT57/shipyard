# Phase 14 — v1.0 Release

## Deliverables

`v1.0.0` tag and GitHub release containing: release notes, an architecture
diagram, a measured benchmark table (from Phase 12, not re-estimated), the
failure-experiment results, known limitations, setup instructions, and a
3-5 minute demo video walking the full loop: issue -> branch -> PR ->
Actions -> Shipyard pipeline -> DAG -> sandbox -> security scan ->
artifact -> trace -> Check result -> review -> merge -> release. README
updated with the pitch, diagram, demo GIF, and results table at the top.

## Career output

Resume bullets built from measured metrics (not aspirational claims), a
short write-up, and a ready list of interview stories: queue delivery
semantics, the sandbox boundary and its limits, propagating a trace
through a queue, the `bisect` debugging story, and one full failure
experiment end to end.
